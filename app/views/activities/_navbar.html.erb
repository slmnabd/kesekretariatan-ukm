<nav class="navbar navbar-default">
  <div class="container-fluid" style="width:80%;">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Home', root_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <%= form_tag root_path, method: :get, class: 'navbar-form navbar-left' do %>
        <div class="form-group">
          <%= text_field_tag :search, nil, class: 'form-control' %>
        </div>
        <%= submit_tag 'cari', class: 'btn btn-default' %>
      <% end %>

      <ul class="nav navbar-nav navbar-right">
        <% if admin_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Menu<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Surat", letters_path %></li>
              <li><%= link_to "Jenis surat", type_of_letters_path %></li>
              <li><%= link_to "Hasil rapat", meeting_results_path %></li>
              <li><%= link_to "Jenis rapat", type_of_meetings_path %></li>
              <li><%= link_to "Pengurus", users_path %></li>
            </ul>
          </li>
        <% end %>
        <% if admin_signed_in? %>
          <li><%= link_to 'Logout', destroy_admin_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to 'Login', new_admin_session_path %></li>
        <% end %>
      </ul>


      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>